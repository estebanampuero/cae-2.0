// This service has been replaced by db.ts using Firestore.
// File kept empty to prevent import errors during transition if lazy loading, 
// though imports should be updated in App.tsx.
export const loadConfigData = () => Promise.resolve([]);
export const loadMedicosData = () => Promise.resolve([]);